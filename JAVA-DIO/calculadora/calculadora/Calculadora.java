package calculadora;

/**
 * Calculadora simples;
 * @author Thales777r
 * @version 1.0
 * @since 2025-11-23
 */
public class Calculadora {
     /**
      * Realiza a divisão entre dois números inteiros.
      * @param dividendo O número que (será dividido).
      * @param divisor O segundo número (que vai dividir).
      * @return o resultado inteiro da divisão.
      */
     // Método vem logo a baixo:
    public int dividir(int dividendo, int divisor) {
        return dividendo / divisor;
     }
    }